//卡特兰数的递推公式为：H[n]=Sigma(H[i]*H[n-i-1],[i,0,n-1])，其还可简化为H[n]=C(2*n,n)-C(2*n,n-1)或者H[n]=C(2*n,n)/(n+1)，且H[0]=H[1]=1
//其主要解决“n多边形三角剖分”、“给定特定进栈顺序，问有多少出栈顺序”、“在圆上选择 2n 个点，将这些点成对连接起来使得所得到的 n 条线段不相交的方法数？”、“n 个结点可构造多少个不同的二叉树？”、“有效括号序列方案数”
void solve(){
    f[0] = 1;
    for (int i = 1; i <= n; i++) f[i] = f[i - 1] * (4 * i - 2) / (i + 1);
}
